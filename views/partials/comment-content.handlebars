<div class="card p-2 m-2">
    <h4 class="card-header">
        <span class="post_header">{{format_date comment.created_at}}</span>
        {{user.username}}
    </h4>
    <p class="card-body">{{content}}</p>
</div>